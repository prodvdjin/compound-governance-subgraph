{
  "name": "compound",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "auth": "graph auth --studio",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ prodvdjin/compound-governance-subgraph",
    "deploy-dev": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ prodvdjin/ecr-subgraph-main",
    "create-local": "graph create --node http://localhost:8020/ prodvdjin/compound-governance-2",
    "remove-local": "graph remove --node http://localhost:8020/ prodvdjin/compound-governance-2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 prodvdjin/compound-governance-2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
